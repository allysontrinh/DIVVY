import { Text, View } from "react-native";

/**
 * Home page of Divvy.
 * Matching Path: /
 * @returns 
 */

export default function HomePage() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
    </View>
  );
}
